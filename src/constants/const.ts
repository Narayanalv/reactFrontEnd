export const defaultHeaders: Record<string, string> = {
    Accept: "*/*",
    "Content-Type": "multipart/form-data",
};

export const ROUTES = {
    HOME: "/",
    REGISTER: "/register",
    MOVIES: "/movies",
};